<div align="center" class="text">
  <h1>Add New Game</h1>
  <% @game.errors.full_messages.each do |error| %>
    <% if !error.include?("team must exist") %>
      <%= error %><br />
    <% end %>
  <% end %>

  <%= render 'form' %>
</div>
